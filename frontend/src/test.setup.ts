// This file runs before every test.
// It loads the extra matchers from jest-dom so we can write things like:
//   expect(element).toBeInTheDocument()
// instead of more complex checks.
import '@testing-library/jest-dom';
